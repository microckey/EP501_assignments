% HW 2 Problem 1(b)

clear
clc
close all

load('testproblem.mat');

% A = [80 -20 -20; -20 40 -20; -20 -20 130]
% b = [20 20 20]'

[L,U] = DLLUF(A,b);

%% Use only backsub function to solve for x

disp('The definition of L & U is A = LU, so')
ALU = L*U;
cat()
